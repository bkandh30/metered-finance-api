PORT=3030
RUST_LOG=info,axum=info,tower_http=info

# Neon DB (MUST use TLS)
DATABASE_URL=postgres://<user>:<password>@<neon-host>/<db>?sslmode=require

# Rate limiting defaults
RATE_LIMIT_PER_MINUTE=120
QUOTA_DAILY_REQUESTS=5000